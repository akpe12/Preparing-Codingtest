import sys

citys = int(sys.stdin.readline().rstrip())
citys_budget = list(map(int, sys.stdin.readline().split()))
budget = int(sys.stdin.readline().rstrip())
result = 0

#a. 도시 요청 예산 총액이 budget을 넘지 않을 때:
#max(citys_budget) 출력
if sum(citys_budget) <= budget:
    print(max(citys_budget))

#b. 도시 요청 예산 총액이 budget을 넘을 때:
#상한액을 정한다(이분 탐색)
#그리고 상한액을 출력
else:
    start = 0
    end = max(citys_budget)
    
    while start <= end:
        mid = (start + end) // 2
        total = 0
        
        for i in citys_budget:
            if mid < i:
                i = mid
            total += i
        
        if total > budget:
            end = mid - 1
        else:
            result = mid
            start = mid + 1
    print(result)
