def alpha_string46(s):
    # 파이썬에 isdigit() 함수가 있다는 걸 기억하자
    return s.isdigit() and len(s) in (4, 6)
    
def solution(s):
    answer = True
    
    for i in s:
        # ascii 수로 58부터 대문자가 나오가
        # 98부터 소문자가 나오기 때문에
        # 문자를 변환했을 때 58이상의 수가 나온다면 바로 false 처리한다.
        if ord(i) >= 58:
            answer = False
    
    if(len(s) != 4 and len(s) != 6):
        answer = False
            
    return answer
