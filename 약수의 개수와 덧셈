Q. 두 정수 left와 right가 매개변수로 주어집니다. 
left부터 right까지의 모든 수들 중에서, 약수의 개수가 짝수인 수는 더하고, 약수의 개수가 홀수인 수는 뺀 수를 return 하도록 solution 함수를 완성해주세요.

def solution(left, right):
    answer = 0
    //정수의 약수의 개수를 담을 리스트 선언
    div = []
    while(left <= right):
        for i in range(1, left + 1):
            #i가 현재 left의 약수라면:
            if left % i == 0:
                #div리스트에 추가
                div.append(i)
        #현재 left의 약수의 개수가 짝수라면
        if len(div) % 2 == 0:
            #총합에 left를 더해준다.
            answer += left
        #약수의 개수가 홀수라면
        else:
            #총합에서 left를 빼준다.
            answer -= left
        #1이 더해진 left의 약수의 개수를 구해야 하므로
        #div를 비워준다.
        div.clear()
        #right와 같은 수가 될 때까지 while문을 돌면서
        #left에 1씩 더해준다.
        left += 1
    
    return answer
