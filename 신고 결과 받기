Q. 각 유저는 한 번에 한 명의 유저를 신고할 수 있습니다.
신고 횟수에 제한은 없습니다. 서로 다른 유저를 계속해서 신고할 수 있습니다.
한 유저를 여러 번 신고할 수도 있지만, 동일한 유저에 대한 신고 횟수는 1회로 처리됩니다.
k번 이상 신고된 유저는 게시판 이용이 정지되며, 해당 유저를 신고한 모든 유저에게 정지 사실을 메일로 발송합니다.
유저가 신고한 모든 내용을 취합하여 마지막에 한꺼번에 게시판 이용 정지를 시키면서 정지 메일을 발송합니다.

- 다른 사람의 풀이

def solution(id_list, report, k):
    answer = [0] * len(id_list)    
    #아이디 리스트의 이름들을 Key로 value는 신고 받은 횟수로 하여
    #reports 딕셔너리를 만든다.
    reports = {x : 0 for x in id_list}

    for r in set(report):
        #신고 명단에 이름이 있으면
        #그 이름을 key에 넣어주어
        #value에 1씩 신고 받은 횟수를 올려준다.
        reports[r.split()[1]] += 1

    for r in set(report):
        #신고 받은 인원인 r의 신고 받은 횟수가 k번을 넘는다면
        #신고한 사람(r.split()[0])의 이름을 id_list에 대조하여 id_list의 어떤 인덱스에
        #신고한 사람이 있는지 찾은 후
        #그 신고한 사람의 이름에 +1을 해준다. (신고 성공 횟수 올려주기)
        if reports[r.split()[1]] >= k:
            answer[id_list.index(r.split()[0])] += 1

    return answer
    
- 나의 풀이

from collections import Counter

def solution(id_list, report, k):
    report = set(list(report))
    accuse = []
    reported = []
    answer = {}
    
    for i in range(len(id_list)):
        answer[id_list[i]] = 0

    for log in report:
        blow, blowed = log.split()
        accuse.append(blow)
        reported.append(blowed)

    reportedNum = Counter(reported)

    for i in range(len(reported)):
        if reportedNum[reported[i]] >= k:
            answer[accuse[i]] += 1

    answer = answer.values()
    
    return list(answer)
