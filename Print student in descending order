#p180
#첫 번째 줄에 등록할 학생 수 N을 입력받는다.
#두 번째 줄부터 학생 이름 그리고 학생의 점수 순서대로 입력받는다.
#점수가 낮은 학생의 이름부터 차례대로 출력되도록 한다.

import sys

#정렬할 때 리스트 내의 [1]번째 값 즉,
#점수를 key로 넘겨주기 위해서
#오름차순 정렬을 하기 위함이다.
def setting(data):
    return data[1]

num = int(sys.stdin.readline().rstrip())
info = []

#한 문장에 str값과 int값을 한 번에 받는 방법
for _ in range(num):
    input_data = input().split()
    info.append((input_data[0], int(input_data[1])))

#[0]번째 값이 아닌 [1]번째 값을 기준으로 오름차순 정렬하는 방법
info = sorted(info, key = setting)
#info = sorted(info, key = lambda student : student[1])
#info.sort(key = setting, reverse = True)

for data in info:
    print(data[0], end = ' ')
