Q. 여러 개의 숫자 카드 중에서 가장 높은 숫자가 쓰인 카드 한 장을 뽑는 게임이다. 단, 게임의 룰을 지키며 카드를 뽑아야 한다.
1. 숫자가 쓰인 카드들이 N X M 형태로 놓여 있다. 이때 N은 행의 개수, M은 열의 개수이다.
2. 뽑고자 하는 카드가 포함되어 있는 행을 선택한다.
3. 그다음 선택된 행에 포함된 카드들 중 가장 작은 숫자의 카드를 선택해야 한다.
4. 따라서 카드를 골라낼 행을 선택할 때, 해당 행에서 가장 숫자가 낮은 카드를 뽑을 것을 고려하여 전략을 세워야 한다.

INPUT: 3 3        OUTPUT: 2
       3 1 2
       4 1 4
       2 2 2

import sys

n, m = map(int, sys.stdin.readline().split())
result = []
#항상 행의 개수만큼의 수만큼 card 행을 채울 것이기 때문에 n번 반복시킨다.
for _ in range(n):
    cards = list(map(int, sys.stdin.readline().split()))
    #입력받은 card의 숫자 중 가장 작은 카드를 result 리스트에 집어넣는다.
    result.append(min(cards))
#입력 받은 각 행별 가장 작은 숫자의 카드들 중 가장 큰 수를 가진 카드를 출력시킨다.
print(max(result))
