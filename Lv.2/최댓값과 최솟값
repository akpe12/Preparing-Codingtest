Q. 문자열 s에는 공백으로 구분된 숫자들이 저장되어 있습니다. str에 나타나는 숫자 중 최소값과 최대값을 찾아 이를 "(최소값) (최대값)"형태의 문자열을 반환하는 함수, solution을 완성하세요.
예를들어 s가 "1 2 3 4"라면 "1 4"를 리턴하고, "-1 -2 -3 -4"라면 "-4 -1"을 리턴하면 됩니다.

- 다른 사람의 풀이

무조건 짧다고 좋은 건 아니겠지만,
map을 사용해서 int와 split()을 한 번만 사용하여
리스트 안의 요소들을 모두 int 처리해줄 수 있다는 게 인상적이었다.

나는 요소 하나하나에 int를 씌워주는 식으로 해야하는 줄로만 알고
for문을 사용해서 처리했다.

def solution(s):
    s = list(map(int,s.split()))
    return str(min(s)) + " " + str(max(s))
    
- 나의 풀이

def solution(s):
    answer = []
    s = s.split(" ")
    ans = ""

    for i in range(len(s)):
        s[i] = int(s[i])

    answer.append(min(s))
    answer.append(" ")
    answer.append(max(s))

    for i in answer:
        ans += str(i)
    
    return ans
